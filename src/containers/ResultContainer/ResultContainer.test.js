import React from 'react'
import { shallow } from 'enzyme'
import ResultContainer from '.'

describe('ResultContainer', () => {
    it('render ResultContainer without errors', () => {
        const wrapper = shallow(<ResultContainer />)
    })
})
